# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def shared_pods
  pod 'FirebaseAuth'
  pod 'FirebaseFirestore'
  pod 'Firebase/Storage'
  pod 'GoogleSignIn'
  pod 'Kingfisher'
  pod 'RealmSwift'
end

target 'Instagram' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  
  # Pods for Instagram
  shared_pods
end

target 'InstagramUITests' do
  use_frameworks!
  
  # Pods for InstagramUITests
  shared_pods
end
